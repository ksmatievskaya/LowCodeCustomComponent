import requests;
import os;
import argparse;

parser = argparse.ArgumentParser(description='Export mesh-applications.')

class LowCodeApplication:
    front_application = dict()
    api_metadata_source = dict()
    model_project = dict()

    def export_to_manifest():
        pass

    def load_from_minifist():
        pass

